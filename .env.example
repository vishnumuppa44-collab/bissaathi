# Add your API key here after copying this file to .env
OPENAI_API_KEY=
# Use an OpenAI-compatible provider if you prefer; retain /v1 at the end where required.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# Leave blank for local development, or choose a private token.
ADMIN_TOKEN=
